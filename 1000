#include<iostream>

using namespace std;

int main() {
	
	int a, b, c;

	cin >> a >> b;

	if (a < 0 && b < 0) {
		return 0;
	}
	else if (a >= 10 && b >= 10) {
		return 0;
	}

	c = a + b;

	cout << c;
}
